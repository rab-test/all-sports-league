# All Sports League

Next.js + Tailwind CSS league site scaffold for the 2026 MVP.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Copy `.env.example` to `.env.local` and set `ADMIN_PASSWORD`.
3. Run the dev server:
   ```bash
   npm run dev
   ```
